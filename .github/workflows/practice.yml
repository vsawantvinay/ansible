name: new practice session on GHA
on:
  push:
    branches:
      - main
jobs:
  Build:
    runs-on: ubuntu-slim
    steps:
      - name: Git check out
        uses: actions/checkout@v7
      - name: cat the file
        run: cat test.txt
