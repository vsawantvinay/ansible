name: varable practice on GHA
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: dispaly varaibels
        run: echo "Hello how are you ${{ vars.MY_NAME }}"
  secret:
    runs-on: ubuntu-latest
    steps:
      - name: deispay secret
        run: echo "my key details are ${{ secrets.KEY_NAME }}"
