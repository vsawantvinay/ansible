name: my GHA steps
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build
        run: echo "build"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "Test"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploy
        run: echo "Deploy"
