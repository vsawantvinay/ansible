name: Test all phase
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Phase
        run: echo "Build"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test phase
        run: echo "Test Phase"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy phase
        run: echo "Deploy"
