name: GHA input section
on:
  workflow_dispatch:
    inputs:
      environement:
        description: select env
        required: true
        default: dev
        type: choice
        options: [dev,qa,stage,prod]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: echo "you are using env as ${{ inputs.environement }}"
