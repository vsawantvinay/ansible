name: Testing on input
on: 
  workflow_dispatch:
    inputs:
      environment:
        description: Choose env
        required: true
        default: dev
        options:
          - dev
          - test
          - prod
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: testing on input
        run: echo "you are running jobs on ${{ inputs.environment}}"
